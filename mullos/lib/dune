(library
 (public_name mullos)
 (libraries
   base
   sedlex
   zarith
   camomile
   batteries)
 (modules
   lexer
   parser
   elab
   syntax
   aux)
 (preprocess (pps sedlex.ppx)))

(menhir
  (flags --explain)
  (modules parser))

(env (dev (flags (:standard -warn-error -a))))
