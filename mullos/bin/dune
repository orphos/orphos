(executables
 (names mullos_main)
 (libraries
   base
   sedlex
   zarith
   camomile)
 (preprocess (pps sedlex.ppx)))

(menhir
  (flags --explain)
  (modules mullos_parser))

(install
 (section bin)
(files (mullos_main.exe as mullos)))
