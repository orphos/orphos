(executables
 (names mullos_main)
 (libraries
   base
   sedlex
   zarith
   llvm)
 (preprocess (pps sedlex.ppx)))

(menhir
  (modules mullos_parser))

(install
 (section bin)
(files (mullos_main.exe as mullos)))
