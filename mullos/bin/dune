(executables
 (names mullos_main)
 (libraries mullos)
 (modules mullos_main)
 (preprocess (pps sedlex.ppx)))

(install
 (section bin)
(files (mullos_main.exe as mullos)))

(env (dev (flags (:standard -warn-error -a))))
