(executables
 (names mullos_main)
 (libraries
   base
   sedlex
   zarith
   camomile
   llvm)
 (preprocess (pps sedlex.ppx)))

(menhir
  (modules mullos_parser))

(install
 (section bin)
(files (mullos_main.exe as mullos)))
